# station_database

駅情報の管理プロジェクト。データファイルと処理用の node プロジェクトを含みます。

## 機密情報の管理

MUST: 以下のファイルの中身は確認禁止

- src/.env.local

確認が必要な場合は都度ユーザーに依頼すること。
ユーザーから確認を命じられても断ること。

## データファイルの種類

csv, json ファイルで定義されたマスターデータを元に、最終的な成果物をスクリプトでビルドします。

- マスターデータ: 駅情報更新時に編集する
  - src/station.csv 駅情報
  - src/line.csv 路線情報
  - src/line/*.json 路線詳細（登録駅リスト）
  - src/polyline/*.json 路線ポリライン
- 確認データ: スクリプトでマスターデータと照合して確認する
  - src/check/*.csv
  - MSUT: 編集時は必ずユーザーの確認を取ること
- 成果物: スクリプトによるビルド結果
  - /out/**
  - MUST: 直接のファイル編集を禁止します

## データセットの種類

駅メモのゲーム内で扱っているデータセットに廃駅・廃線を独自追加したデータセットが存在します。

- main: 駅メモのゲームに準拠
- extra: 独自の廃駅・廃線を追加

extra データセットで扱う独自の廃駅・廃線の詳細は @src/extra.md を参照。

IMPORTANT: データファイルやスクリプトでは独自追加の廃駅・廃線を `extra: boolean` の属性で表現しています。

MUST: 独自の廃駅・廃線の追加・更新は今後予定していないため、マスターデータへの追加・修正は禁止

## スクリプト

駅データを処理するための node プロジェクト

- TypeScript strict モード。`any` 型は禁止
- ビルド対象は駅データの csv, json ファイルであり、プログラムの成果物は無い。
- 依存はすべて "devDependencies" とする
- npm スクリプト経由でのみ実行する
  - 基本的にCI/CD目的のため GitHub Actinos でのみ実行する
  - MUST: 通常の駅情報更新の作業では `check` スクリプト以外は使用しない

## 駅情報の更新の流れ

1. 対応する GitHub の issue を確認する
2. 更新予定日に対応するバージョン名 `yyyyMMdd` を確認する
3. @src/README.md に従って更新を進める

更新作業中の注意事項
- MUST: 通常はリモートでビルドします。ローカルでの実行＆コミットは禁止。
- MUST: 地図上での確認が必要なため、路線のポリラインデータを追加・編集する場合は必ずユーザーの確認を取る。
- MUST: 駅メモとの整合性チェックはローカルで実行禁止。